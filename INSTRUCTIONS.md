VS Code Instructions:

Run this command in a terminal:
node server.js

Open system_monitor_daemon.py and click Run > "Run Without Debugging" or run system_monitor_daemon.py in a new terminal, with the other terminal still active

Open http://localhost:3000/ in your browser